<?php
/**
 * Functions to capture the data from the
 * taxonomies CRUD form, process/manipulate
 * that data, store it, and use it to
 * register/edit a a custom taxonomy.
 *
 * @since      0.1.0
 * @package    UTK_Asset_Manager
 * @subpackage includes/process-assets
 */

if ( ! defined( 'WPINC' ) ) {
	die;
}